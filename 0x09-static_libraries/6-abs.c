#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @i: int to be checked
 * Return: absolute value of int
 */
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
